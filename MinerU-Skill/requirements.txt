# MinerU Skill — runtime dependencies
#
# NONE. The core tool (scripts/mineru.py) uses only the Python standard library
# so any environment with `python3` can run it with zero install. This is a
# deliberate "AI-Native" choice: an agent can invoke the tool instantly.
#
# Development / testing only (install with: pip install -e ".[dev]"):
#   pytest>=7.0.0
#   ruff>=0.1.0
#   black>=23.0.0
